// Update with your credentials
const DB_USER = ''; 
const DB_PASSWORD = '';
const DB_SERVER = 'localhost';
const DB_DATABASE = 'BUDGET';

const backendAPI = 'http://localhost:3001/'; // Update to the port where your backend is set to run by default
const frontend = 'http://localhost:3000'; // Update to the port where your frontend is set to run by default

module.exports = {
    DB_USER,
    DB_PASSWORD,
    DB_SERVER,
    DB_DATABASE,
    backendAPI,
    frontend
};
